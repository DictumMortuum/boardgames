import { defaultTheme } from 'react-admin';

const theme = {
  ...defaultTheme,
  palette: {
    background: {
      default: '#fafafb',
    },
    primary: {
      main: '#5e81ac',
    },
    secondary: {
      main: '#3b4252',
    },
  },
};

export default theme;
